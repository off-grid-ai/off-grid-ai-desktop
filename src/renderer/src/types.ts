// Shared renderer types that both core and the pro package reference.

import type { SearchResult } from '../../shared/search-contract'

/** Renderer name for the shared universal-search result contract. */
export type SearchHit = SearchResult
